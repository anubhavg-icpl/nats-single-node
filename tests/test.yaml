---
- name: Test NATS role
  hosts: all  # Changed from 'core' to 'all' to match any host in inventory
  become: yes
  roles:
    - role: ../../ansible-role-nats
      vars:
        nats_host_group: core  # Explicitly set the host group